const usersReducer = (state = [], action) => {
    switch (action.type) {
        case '':
            return (
                Object.assign({}, state, {
                    
                })
            )
        default:
            return state 
    }
}