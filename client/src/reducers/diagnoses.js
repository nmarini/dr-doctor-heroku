const diagnosesReducer = (state = [], action) => {
    switch (action.type) {
        case '':
            return (
                Object.assign({}, state, {

                })
            )
        default:
            return state 
    }
}

export default diagnosesReducer